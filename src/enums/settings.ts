/**
 * 側邊欄狀態列舉
 */
export const enum SidebarStatus {
  // 展開
  OPENED = "opened",
  // 關閉
  CLOSED = "closed",
}

/**
 * 語言列舉
 */
export const enum LanguageEnum {
  // 繁體中文
  ZH_TW = "zh-tw",
  // 英文
  EN = "en",
}

/**
 * 裝置列舉
 */
export const enum DeviceEnum {
  // 寬屏裝置
  DESKTOP = "desktop",
  // 窄屏裝置
  MOBILE = "mobile",
}
